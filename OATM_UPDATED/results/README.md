# Results

Compact, reviewable experiment outputs belong here. Each report must state
whether evidence is synthetic, detector-intervention, controlled visual, or
natural; these families must never be merged into one headline score.

Large row-level tracker outputs and detector caches belong in `artifacts/` and
remain untracked.
