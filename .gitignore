# nuScenes dataset and the private review workbook -- must stay local
data/
occlusion_samples.xlsx

# Generated outputs -- regenerable from scripts, never uploaded
# occluded_samples/
# results/

# Isolated Python environment for the YOLO experiment
**/.venv/

# Assignment 4 uv environment (kept explicit for local setup clarity)
assignments/04_bytetrack_paper_experiment/experiment/.venv/

# Pretrained model weights (downloaded, not authored here)
*.pt

# Python cache
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/

# SORT experiment: raw copied nuScenes clip frames and the detection cache
# are regenerable build artifacts, not reviewable results -- never commit them.
assignments/03_sort_paper_experiment/results/clips/
assignments/03_sort_paper_experiment/results/.detection_cache.json
assignments/03_sort_paper_experiment/results/*.bak
assignments/03_sort_paper_experiment/experiment/**/__pycache__/
assignments/03_sort_paper_experiment/experiment/.pytest_cache/

# ByteTrack experiment: same regenerable build artifacts as Assignment 3 --
# raw copied clip frames, the detection cache, and large local-only visual
# review material (overlays/contact sheets, reproducible from the committed
# CSVs and scripts). Never commit them.
assignments/04_bytetrack_paper_experiment/results/clips/
assignments/04_bytetrack_paper_experiment/results/.detection_cache.json
assignments/04_bytetrack_paper_experiment/results/*.bak
assignments/04_bytetrack_paper_experiment/results/projection_overlays/
assignments/04_bytetrack_paper_experiment/results/natural_event_contact_sheets/
assignments/04_bytetrack_paper_experiment/experiment/**/__pycache__/
assignments/04_bytetrack_paper_experiment/experiment/.pytest_cache/
